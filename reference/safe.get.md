# function saftly return name from enviroment(s) and don't throw error if name don't exists

function saftly return name from enviroment(s) and don't throw error if
name don't exists

## Usage

``` r
safe.get(name, env)
```

## Arguments

- name:

  \- string

- env:

  \- environment or list of environments
