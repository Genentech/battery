# helper function that print the context in which the error was thrown

helper function that print the context in which the error was thrown

## Usage

``` r
report.error(error, meta = NULL)
```

## Arguments

- error:

  \- error structure created by `create.error`

- meta:

  \- context of the error (origin, type, ...)
