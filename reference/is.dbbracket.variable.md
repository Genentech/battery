# Function check if argumnet is expression foo\[\[bar\]\]

Function check if argumnet is expression foo\[\[bar\]\]

## Usage

``` r
is.dbbracket.variable(item)
```

## Arguments

- item:

  \- substitute expression
