# function restart the evaluation of the code after the signal to stop the propagation of event handlers in nested withHandlers

function restart the evaluation of the code after the signal to stop the
propagation of event handlers in nested withHandlers

## Usage

``` r
continue()
```
