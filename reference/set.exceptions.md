# helper function that set exception uncoditionaly, old handler is discarded

helper function that set exception uncoditionaly, old handler is
discarded

## Usage

``` r
set.exceptions(handler, session = NULL)
```

## Arguments

- handler:

  \- named list of exception handlers

- session:

  \- optional shiny session, if omitted the global handlers are set
