# Function return true if function is internal - used to prevent infinite recursion

Function return true if function is internal - used to prevent infinite
recursion

## Usage

``` r
is.internal.function(fn.body)
```

## Arguments

- fn.body:

  \- body of the function
