# helper function that returns exception handler

helper function that returns exception handler

## Usage

``` r
get.exceptions(session = NULL)
```

## Arguments

- session:

  \- optional shiny session, if omitted the global handlers are used
