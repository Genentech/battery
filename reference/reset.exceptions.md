# helper function that reset exceptions

helper function that reset exceptions

## Usage

``` r
reset.exceptions(session = NULL)
```

## Arguments

- session:

  \- optional shiny session, if omitted the global handlers are reset
