# Higher order function for creating functions for checking if item is foo\$bar or foo\[\[bar\]\]

Higher order function for creating functions for checking if item is
foo\$bar or foo\[\[bar\]\]

## Usage

``` r
is.variable(chr)
```

## Arguments

- chr:

  \- character to match as first item in expression
