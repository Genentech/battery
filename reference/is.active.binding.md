# Function check if name is active biding inside environment activeInput or activeOutput

Function check if name is active biding inside environment activeInput
or activeOutput

## Usage

``` r
is.active.binding(name, env)
```

## Arguments

- name:

  \- string

- env:

  \- active environment to test
