# helper function that clean message and remove classes from cond object

helper function that clean message and remove classes from cond object

## Usage

``` r
clean(cond)
```

## Arguments

- cond:

  \- condition object
