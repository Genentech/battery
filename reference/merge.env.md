# Helper function that create single environment from environments in arguments

Helper function that create single environment from environments in
arguments

## Usage

``` r
# S3 method for class 'env'
merge(...)
```

## Arguments

- ...:

  \- any number of environments objects
