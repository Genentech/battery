# Helper where new static default values can be added this static env should be the same for each Component per shiny session

Helper where new static default values can be added this static env
should be the same for each Component per shiny session

## Usage

``` r
new.static.env()
```
