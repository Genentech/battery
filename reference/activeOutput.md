# Function create active binding output mock to be used with renderUI mock

Function create active binding output mock to be used with renderUI mock

## Usage

``` r
activeOutput(...)
```

## Arguments

- ...:

  \- named initial active inputs
