# Main file with base of battery components Global env generator function - added to each component one per root (component without parent) This env is used so you can share services for one tree of components. You can't send data between Two trees of components.

Main file with base of battery components Global env generator
function - added to each component one per root (component without
parent) This env is used so you can share services for one tree of
components. You can't send data between Two trees of components.

## Usage

``` r
new.global.env()
```
