# Recursive function that get nested object and prop name for give object access exression

Recursive function that get nested object and prop name for give object
access exression

## Usage

``` r
get.object(item, env)
```

## Arguments

- item:

  \- substitute expression

- env:

  \- environment for this expression
